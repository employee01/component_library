<!doctype html>
<html lang="{{ request.locale.iso_code }}" lang="{{ request.locale.iso_code }}">
  <head>
    {{ content_for_header }}

    {% import "../assets/global.css" | inline %}
  </head>

  <body>
    <main id="main" role="main" class="flex-1">
      {{ content_for_layout }}
    </main>
  </body>
</html>
